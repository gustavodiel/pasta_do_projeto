<div class="card mx-auto" style="width: 40%; min-width: 200px; max-width: 512px; margin-top: 1em">
  <div class="card-header">
    <i class="bi-card-text"></i> Imagem
  </div>
  <%= image_tag @image.file.representation(resize_to_limit: [256, 256]), class: 'card-img-top' %>

  <div class="card-body">
    <h5 class="card-title"><%= @image.name %>!</h5>
    <p class="card-text">Cor secundária</p>
    <p class="card-text">Cor de detalhe</p>
    <div class="btn-group">

      <%= link_to 'Editar', edit_image_path(@image), class: 'btn btn-warning' %>
      <%= link_to 'Voltar', images_path, class: 'btn btn-secondary' %>
    </div>
  </div>
</div>
